# Rename this to secrets.toml locally.
# On Streamlit Cloud, add these under App settings → Secrets.

COOKIE_PASSWORD = "replace-with-long-random-string"

# For local FastAPI / ngrok / deployed API:
DATA_API_BASE = "https://your-data-api.example.com"
AUDIO_API_BASE = "https://your-audio-api.example.com"
